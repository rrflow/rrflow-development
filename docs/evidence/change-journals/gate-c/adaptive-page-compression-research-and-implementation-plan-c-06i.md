# Adaptive page-compression research and implementation plan (C-06i)

**Status:** active immutable change-journal evidence
**Coordinate:** `rrflow://rrflow-instance/data/evidence/change-journal/gate-c/adaptive-page-compression-research-and-implementation-plan-c-06i`
**Owner:** the unchanged historical package receipt identified by its legacy heading and payload digest
**Legacy source:** `docs/roadmap/rrflow-1.0-execution-map.md@b7b061900b67d535ffec2710e6e4c30815d0f216#L3861`
**Legacy payload SHA-256:** `b921d1ab0b503d8979d02b4871e6404a6e38e9800f5d77c1a83e5fe23b70e499`

This record refracts one completed package receipt out of the former
monolithic execution map. Its fenced payload is preserved byte for byte.
It reports historical evidence and cannot change roadmap completion or
POA&M lifecycle status. Follow the [canonical roadmap](../../../roadmap/rrflow-1.0.md)
for acceptance and the [change-journal index](../) for discovery.

## Journal payload

```text
gate/package: C-06i-adaptive-page-compression-research-plan / segment-v6 production decision and exact implementation map
alpha outcome or prerequisite advanced: turns the measured codec candidate into a reviewable production design for the sole rrflowKV substrate. It changes no runtime, durable byte, dependency, fixture, canonical gate status, DataFusion source, install path, binary, or release claim
starting revision/tree/branch/remote/worktree: clean baseline 787c618b659461ca9a72eed5db78aa9c2e41b28b / c4750d8a9aee7942d64e114d2e05f40cedb3b6f0; planning commit 58cc8f882186a25e080ee2d85e344dc367bd5ed8; branch agent/connectome-temporal-runtime-visualizer; development target development/main at https://github.com/rrflow/rrflow-development.git; official origin https://github.com/rrflow/rrflow.git remains promotion-only; Cargo.lock SHA-256 316533e7512dbb9029e494386503ca8430b0c69853b3b78ef02319dcbab93a27
baseline files/digests: rrflow-1.0-active-change.json authenticates the complete owning README/AGENTS/roadmap/research/current-format/benchmark/candidate/evidence records, rrd-lsm manifest/lib/database/segment format/module/reader/laboratory and segment/hybrid/tiered/snapshot tests, current segment-v5 fuzz target, workspace architecture oracle, execution map, and inventory generator against the clean baseline. The planning-only commit passed check_change_plan with zero post-plan paths
change brief (current -> target behavior, owner, exact scope, unchanged behavior, stop conditions): current segment v5 stores all six aligned Arrow-layout pages raw despite retaining separate physical/logical lengths and a codec byte. Specify a direct v6 replacement with authenticated writer policy, per-page none/LZ4-block identity, checked 1,250-basis-point adaptive selection, raw mmap borrowing, checksum-before-decode, exact owned aligned decode, a reconstructed 1 GiB logical envelope, decoded-byte cache charging, low-cardinality evidence, direct fixture/fuzz replacement, and none/adaptive independent-model proof. Preserve all runtime code and bytes in this research package. Stop on unresolved format/bounds/ownership/corruption behavior, compatibility, hidden fallback, DataFusion storage authority, undeclared candidates, overclaim, or official-repository action
files read in full: README.md, AGENTS.md, the active package it replaced, canonical roadmap, roadmap index, physical-policy and persisted-filter plans, 661-line storage research, 559-line current-format reference, 220-line benchmark reference, 966-line candidate artifact, rrd-lsm Cargo manifest/lib/database/segment format/module/reader/laboratory, segment/hybrid/tiered/snapshot/manifest tests, segment-v5 fuzz target, complete workspace architecture test, complete execution map, and complete inventory generator. Each changed document and final diff is reread before closure
research decision and primary-source/adaptation record: required. The pinned LZ4 block specification contributes independent block and caller-owned bound semantics; pinned lz4_flex 0.14.0 contributes safe checked block encode/decode; Arrow contributes independent buffer/alignment constraints; Parquet contributes the useful separation of stored/logical lengths and stored-byte checksum; RocksDB contributes per-block raw fallback, hot/cold codec, decoded-cache, scan, and pinning questions; DataFusion contributes execution-stream I/O/pushdown and explicit MemoryPool exclusions; Moka contributes only a later TinyLFU comparison. No upstream tree, format, public API, compatibility surface, option model, benchmark conclusion, or runtime is copied
trace/resource/debug decision (add/preserve/not applicable, with reason): future trace=add policy kind/threshold and raw/compressed/stored/logical/scratch aggregates to existing write/open owners; future resource evidence=add a format-level logical envelope, exact authenticated decode length, decoded cache charge, and separate physical/decoded/borrowed/allocated/copied/decompressed counters; future debugging=add checksum-rewritten policy/codec/length/data corruption, none/adaptive differential, mixed ownership, frozen v6 vector, manifest/snapshot closure, and structure-aware fuzz cases. This planning package emits no runtime event
first-failure or characterization oracle and result: current decode_page_descriptor rejects every codec except zero and requires physical_bytes == logical_bytes; current append_page always writes raw and hashes those bytes; decompressed counters remain zero. The clean candidate screen measured exact LZ4 and Zstandard round trips and selected 677/834 pages for each, but did not define production metadata, bounds, allocation, ownership, corruption, or DataFusion handoff. That absence is the first failure and the new plan resolves each item without changing production code
files changed/created/deleted/moved: create c06i-rrflowkv-adaptive-page-compression-engineering-plan.md; update the storage research synthesis, roadmap index, completed persisted-filter handoff, and this journal; regenerate rrflow-1.0-file-plan.jsonl. No source, dependency, lockfile, fixture, fuzz asset, API, generated surface, version, binary, or runtime path changes
contract or behavior changed: documentation and implementation authorization only. The selected future default is adaptive LZ4 block compression with configurable none mode and direct segment-v6 replacement. Segment v5 remains the actual sole runtime format until a new machine-bound implementation package passes. C-06, F-01, F-05, J-04, POAM-002, and every alpha outcome remain open
smallest test command and result: python3 scripts/ci/check_change_plan.py passes and recognizes exactly six declared post-plan paths at planning revision 58cc8f8; the initial planning-only run passed with zero post-plan paths
owning package command and result: deterministic inventory generation/check passes with 963 current/generated/planned records; documentation policy passes with 98 statuses, 96 classified coordinates, parent indexes, and local links
cross-boundary command and result: workflow policy passes 3 workflows, 7 substantive jobs, 5 cohesive engine suites, 20 default-feature packages, and 6 optional-feature packages; version policy retains 1.0.0; all 11 knowledge-export tests pass; generated parity retains 33 HTTP operations at OpenAPI SHA-256 1d18655aa6e670abd7319c3984dfc36b8ed50c280ce8afb62322a5e062b14cc6; Cargo formatting and staged/unstaged diff integrity pass
failure/crash/differential evidence: no runtime failure, crash, or differential suite is run because this package changes no executable behavior. Full design review found that lz4_flex compress_into requires its documented worst-case output capacity: the plan now authenticates a configurable maximum compressible logical page, defaults it to 16 MiB, leaves larger pages raw without codec scratch, and records scratch high-water bytes. The first documentation check rejected the completed filter record because its historical status used a Successor field instead of the required Superseded by link; the field was corrected without changing its evidence. One read-only search command contained an unmatched shell backtick and did not launch the search; the literal-free command was rerun successfully. The runtime plan still requires checksum-rewritten codec corruption, exact none/adaptive MVCC/lifecycle differential, all publication boundaries, current-format fuzzing, and clean fixed-machine evidence before the production slice can close
full-file reread and diff review: complete. The complete new 563-line engineering plan and complete resulting roadmap index/filter record were read. The complete research and execution-map baselines had already been read in this package; every resulting changed section and the complete staged delta were then read, which covers every final line as baseline plus reviewed delta. The generated JSONL was regenerated from the staged tree, parsed by its generator/checker, and its complete 11-line delta was reviewed. Final review found no runtime edit, duplicate authority, unresolved codec byte, unchecked design arithmetic, unauthenticated decode length, compressed-zero-copy claim, DataFusion storage authority, compatibility reader, hidden candidate, status overclaim, or undeclared path
change checklist: baseline=complete; authority=complete; scope=complete; full reads=complete as baseline plus final deltas; first failure=complete; traceability=complete; primary research=complete; byte/API/ownership/test design=complete; documentation edits=complete; validation=complete; reread=complete; research-package commit/push=complete; runtime implementation=not started by design
not run and reason: all Rust tests, Clippy, fuzzing, benchmarks, SDK/API/install/server/MCP/Connectome/release suites are outside this documentation-only package because it changes no executable or generated surface. The separately planned runtime package owns the relevant focused, package, cross-boundary, fuzz, failure, and fixed-machine runs
remaining known errors: segment v5 pages are still uncompressed; no segment-v6 runtime exists; cache admission, value placement, continuous/cross-platform storage qualification, installed binary, native graph/BM25/vector/TurboQuant, stamped DataFusion provider, reasoning/context/routines/skills, SDK/API/Connectome, signed bundle, and release proof remain open
commit/development push evidence: planning commit 58cc8f882186a25e080ee2d85e344dc367bd5ed8 and research-package commit 96f3630582eb227f9ce07e0e8bfab1a68a376d87 were created normally without bypassing repository validation. Immediately before the non-force push, development resolved to https://github.com/rrflow/rrflow-development.git, official origin resolved separately to https://github.com/rrflow/rrflow.git, development/main resolved to baseline 787c618b659461ca9a72eed5db78aa9c2e41b28b, official main resolved to 8406e7114b7f7887e9f7ac4387df94184638eeca, and the candidate was exactly 96f3630582eb227f9ce07e0e8bfab1a68a376d87. The normal `HEAD -> main` fast-forward succeeded and `git ls-remote` verified development/main at exactly 96f3630582eb227f9ce07e0e8bfab1a68a376d87 while official main remained unchanged. This entry is an evidence-only successor; no official ref, tag, binary, artifact, or release was changed or authorized
roadmap checkbox changed: no
```
