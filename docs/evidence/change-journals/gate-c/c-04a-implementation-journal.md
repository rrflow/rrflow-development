# C-04a implementation journal

**Status:** active immutable change-journal evidence
**Coordinate:** `rrflow://rrflow-instance/data/evidence/change-journal/gate-c/c-04a-implementation-journal`
**Owner:** the unchanged historical package receipt identified by its legacy heading and payload digest
**Legacy source:** `docs/roadmap/rrflow-1.0-execution-map.md@b7b061900b67d535ffec2710e6e4c30815d0f216#L2345`
**Legacy payload SHA-256:** `6752ea9d70c740c72f58be243de524b1088da122635c84de0685ceade238ae22`

This record refracts one completed package receipt out of the former
monolithic execution map. Its fenced payload is preserved byte for byte.
It reports historical evidence and cannot change roadmap completion or
POA&M lifecycle status. Follow the [canonical roadmap](../../../roadmap/rrflow-1.0.md)
for acceptance and the [change-journal index](../) for discovery.

## Journal payload

```text
gate/package: C-04a / complete version-key closure and direct-access contract; package complete, C-04 gate still open
starting revision: eb53fe2da1e7bda235ec3481d1cf2cff95c92ab3
baseline files/digests: keyspaces.rs=a0d6a95ffdfe05b4720b50a02f1f2b1c229ad074771c42bedfc44d91a5558847; access/mod.rs=0980a1519cdc4642feb9840fd2a1f1b1a12814732e542a6ed3ebaf4b4a87eeaa; access/runtime_state.rs=91adae340b0be207d65ddd33a9de97da0fda900f779e3a423dccfa3161c99507; access/semantic_commit.rs=31e9a94d69314609dac3199ae515297ef9e858f590527b560fbd52c2c1980659; access/vector.rs=cc1e33f64a2c7406b2e19bb7cec2eda0af70741659f8aa1115974c81984d82f2; error.rs=f3e1648c1cf35fc7078ef164d997423aa9180ec1bf131b4e0cb04f791082828f; lib.rs=2963486c0242b70ef71ef802b1bf28cd68ee1395f9d0b667294fdc9fc1c217a7; repository/runtime.rs=0c6ffd650ae1828a052a89f6331a082d949964f72d9c9ad19cd2fb205d0f1b2e; tests/snapshot.rs=7e87717eb911fd15a4b543cdeda795a991aa13d13d0ec57a609539f362bc1d2f
files read in full: repository AGENTS instructions; root README and its objective/roadmap/POA&M/data-flow/persistence-scenario owner chain; rrd-store access root, runtime-state access, semantic commit, vector access, keyspaces, errors, exports, transaction port, storage profiles, runtime repository, semantic-commit/native-index/key-codec/unified-data/snapshot tests, and every newly authored C-04a source/test/fixture. C-04a's public repository method required adding repository/runtime.rs and its snapshot assertion to the planned file set before this commit; it does not migrate data_snapshot or query behavior owned by C-04b/C-04c
files changed/created/deleted/moved: changed rrd-store/src/access/{mod,runtime_state,semantic_commit}.rs, src/{error,keyspaces,lib}.rs, src/repository/runtime.rs, tests/snapshot.rs, and this execution record; created src/access/versioned_read.rs, tests/direct_read_paths.rs, and fixtures/rrflow-kv-direct-version-keys-v1.hex; no file deleted or moved
contract or behavior changed: SemanticCommitPlan now writes one scope-bound typed semantic version for every schema, claim, record, relation, event, vector, series sample, geo value, object reference, and permitted typed retirement while retaining current heads and the authoritative causal log. RuntimeRepository exposes one profile-neutral read_versioned operation over a caller-supplied ReadStamp, typed family/kind/predicate selections, and nonzero caller-supplied key budget. The reader rejects overlapping ranges, validates every physical key against its decoded mutation, ignores post-stamp versions, authenticates every selected change against the stamped RFC 9162 root, and returns ordered closed point/range/key/value/byte evidence. Historical stamp validation resolves the retained head by authenticated point and the schema by direct descending version seek; it no longer reconstructs schema state from cursor zero. No fixed workload policy, alternate backend, external fetch, provider authority, DataFusion write path, compatibility reader, or second state engine was added
smallest test command and result: cargo test -p rrd-store --lib access::versioned_read::tests --locked passed 2 cases: wrong-key/wrong-family denial and overlapping-range denial. cargo test -p rrd-store --test direct_read_paths --locked passed the one full differential case. Its first run correctly failed because the test object receipt used a noncanonical content key; the fixture was corrected to ObjectReference::canonical_key and then passed
owning package command and result: cargo test -p rrd-store --locked --quiet passed all 171 unit/integration cases. cargo clippy -p rrd-store --all-targets --locked -- -D warnings passed. The frozen direct-version key fixture, semantic write closure, historical/current snapshot equality, forged-root denial inherited by snapshot tests, key-budget failure, type narrowing, mixed-scope isolation, and rrflowKV flush/reopen all pass
cross-boundary command and result: cargo check --workspace --all-targets --locked passed every workspace target. cargo test -p rrd-query --test query --locked surfaced exactly one scheduled C-04c assertion failure while 14/15 cases passed: query.rs:398 still requires the removed full_hash_chain_replay label after the live head advances. The executable now truthfully returns rfc9162_direct_versions; query execution itself still replays and is not being relabelled or patched in C-04a
failure/crash/differential evidence: the fixed corpus contains two schema revisions, claims, records, a relation, event, vector, series sample, geo value, object reference, seven typed retirements, and an interleaved foreign scope. The retained-stamp direct result equals all 10 scoped authenticated-log oracle changes and its valid-time snapshot; the current result equals all 18 scoped oracle changes and its post-retirement snapshot. rrflowMX and reopened rrflowKV produced byte-for-byte equal logical evidence. Observed retained evidence was 49 point reads, 10 ranges, 68 keys/values, and 15,371 decoded bytes; current evidence was 89 point reads, 9 ranges, 107 keys/values, and 17,109 decoded bytes. Around reopened rrflowKV direct reads, physical counters increased by 9 block loads, 16,128 loaded bytes, and 164 filter checks. The direct reader contains no RUNTIME_CHANGES range; source closure finds remaining cursor-zero reconstruction only in retirement validation and missing-accumulator bootstrap, owned respectively by C-04b and C-05
not run and reason: full workspace tests, SDK/transport/Connectome conformance, C-06 Arrow-page work, F DataFusion streaming, E index quality, install/attunement, deployment, benchmark threshold, and release suites were not run because C-04a changes only rrd-store version publication/direct access. The complete workspace compiled; the focused downstream query suite was intentionally run to expose the next boundary
remaining known errors: C-04b must make data_snapshot, transaction previews, and retirement validation consume this reader; C-04c must replace normal rrflowQL/vector/retrieval/context replay and update the one surfaced query assertion with direct-plan evidence. Accumulator bootstrap replay remains C-05 inventory. C-05 through J and the alpha outcomes remain open exactly as their owners state
roadmap checkbox changed: no; C-04a is complete evidence only, C-04 remains unchecked, Gate C remains 3/7, and C-04b is the next executable package
```
