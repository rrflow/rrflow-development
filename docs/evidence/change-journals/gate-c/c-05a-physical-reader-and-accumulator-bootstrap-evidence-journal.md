# C-05a physical-reader and accumulator-bootstrap evidence journal

**Status:** active immutable change-journal evidence
**Coordinate:** `rrflow://rrflow-instance/data/evidence/change-journal/gate-c/c-05a-physical-reader-and-accumulator-bootstrap-evidence-journal`
**Owner:** the unchanged historical package receipt identified by its legacy heading and payload digest
**Legacy source:** `docs/roadmap/rrflow-1.0-execution-map.md@b7b061900b67d535ffec2710e6e4c30815d0f216#L2480`
**Legacy payload SHA-256:** `a53cd9553bdc9a32504192ddf02723601be34383b943ceb7e23183bf0af3eec1`

This record refracts one completed package receipt out of the former
monolithic execution map. Its fenced payload is preserved byte for byte.
It reports historical evidence and cannot change roadmap completion or
POA&M lifecycle status. Follow the [canonical roadmap](../../../roadmap/rrflow-1.0.md)
for acceptance and the [change-journal index](../) for discovery.

## Journal payload

```text
gate/package: C-05a / direct removal of pre-1.0 low-level readers and cursor-zero accumulator reconstruction; bounded evidence only, C-05 remains open
revision: starting revision 65936ac5a56630cac0137f11b69a718545791384; result is the commit containing this entry
baseline files/digests: inherited unchanged from the immediately preceding C-05a plan journal; that record was written before implementation deletion and is the implementation-requirements trace for this package
files read in full: every file listed by the preceding plan journal; additionally the complete rrd-lsm tests/snapshot_bundle.rs before its cross-boundary rejection assertion; every changed hunk and the complete post-change current-format and engine-data-flow records were reviewed. The post-change default-source search classified recovery terminology, intentional schema-evolution descriptions, optional S3 compatibility, upstream-reference comments, hyper's dependency-owned legacy module path, later-gate kernel/estate/vector residues, and direct rrflowKV/cluster openers rather than hiding them behind a zero-match claim
files changed/created/deleted/moved: changed rrd-lsm src/{batch,manifest,segment}.rs and tests/{manifest,mvcc,segment,snapshot_bundle}.rs; changed rrd-store access/{runtime_state,semantic_commit}.rs; changed docs/reference/storage/rrflowkv-current-format.md, docs/architecture/engine-data-flow.md, the POA&M, this execution journal, and the deterministically generated file plan. No file was created, deleted, moved, renamed, aliased, or archived; no dependency, version, public endpoint, SDK, provider adapter, installation behavior, release artifact, or official-repository state changed
contract or behavior changed: WriteBatch decoding now accepts only RRDBAT02/version 2; manifest validation and digesting now accept only version 2; segment direct open, manifest-addressed reopen, snapshot validation, and snapshot installation now accept only RRDSEG03/version 3. Earlier and unknown numeric versions return the explicit UnsupportedVersion error before any alternate decoder; same-version bad magic/framing remains invalid data. Segment directly owns its one block source/table/filter/cache representation; the old representation enum, sparse reader API, v1/v2 decoder, validators, seek, and visitor are absent. A semantic commit at a nonzero cursor now requires the stored authenticated RuntimeLogAccumulator and fails closed when it is absent; it never rebuilds state by replaying RuntimeChanges from cursor zero. Cursor-zero creation still initializes normally
smallest test command and result: cargo test -p rrd-lsm --test mvcc --test manifest --test segment --locked passed 30/30, including exact v1 batch/manifest and v1/v2/unknown segment rejection. The first exact semantic-filter command ran zero tests because the module-qualified name was omitted and was discarded; cargo test -p rrd-store nonempty_runtime_never_rebuilds_a_missing_accumulator_from_the_change_log --locked then passed the one intended test across rrflowMX and rrflowKV reopen. cargo test -p rrd-lsm --test snapshot_bundle corruption_and_truncation_are_denied_before_manifest_publication --locked -- --exact passed 1/1 and proves a pre-1.0 segment embedded in a real bundle is rejected before target-manifest mutation
owning package command and result: cargo test -p rrd-lsm --all-targets --locked passed all 76 tests; cargo test -p rrd-store --all-targets --locked passed all 172 tests and examples; cargo clippy -p rrd-lsm -p rrd-store --all-targets --locked -- -D warnings passed
cross-boundary command and result: cargo test -p rrd-engine --test workspace_architecture --locked passed all 24 source/dependency/authority guards; cargo check --workspace --all-targets --locked passed all enabled targets. Locked Cargo metadata resolves rrd-lsm only to crc32c, io-uring, libc, lz4_flex, memmap2, ring, rrd-core, serde, serde_json, tempfile, tracing, and windows-sys, and rrd-store only to rrd-core, rrd-lsm, serde, serde_json, tempfile, and tracing. Corrected exact dependency/source probes found no Fjall/RocksDB/Sled/Redb/SurrealDB persistent-store dependency and no removed reader/fallback symbol. Deterministic inventory regeneration and recheck passed with 909 current/generated/planned records; documentation passed with 90 statuses and 88 classified coordinates; generated-surface parity retained 33 HTTP operations at OpenAPI digest 786e633fb850d9de13d96754d8929618a1bcea9d1184d8efdf02ed819b3e48ef; workflow policy, frozen 1.0.0 version, all 11 knowledge-export tests, Cargo formatting, and diff integrity passed
failure/crash/differential evidence: low-level failure matrices, WAL rotation/recovery, manifest publication, segment corruption, snapshot install/export interruption, compaction, bounded cache/I/O, transaction conformance, and rrflowKV close/reopen all remain green. A real nonempty rrflowMX store and a closed/reopened rrflowKV store both reject a deleted accumulator at cursor 7 instead of deriving it from the log. The batch v1 bytes reach WAL recovery but fail before memtable application; manifest v1 fails validation; segment v1/v2 and unknown v4 fail direct open; an embedded segment v2 marker fails snapshot validation and installation without publishing the target manifest
surfaced failures and corrections: an early segment edit typo was caught by immediate source inspection before compilation. The first module-unqualified exact test selected zero tests and was replaced by a command that selected the intended case. A test process lost its continuation session before a final exit status and the complete rrd-store suite was rerun from the start. One documentation search had an unmatched shell backtick and made no change; its corrected form ran. One multi-file documentation patch missed wrapped context and made no change; smaller verified hunks applied. One dependency probe used an unexpanded double-star path and was explicitly discarded despite its misleading else branch; the corrected --glob probe passed. The pre-journal inventory check correctly failed only for expected deterministic drift
not run and reason: the complete workspace test suite, optional distributed-feature/process qualification, external SDK conformance, Connectome, installation/attunement, provider/mesh/application-database adapters, C-06 hybrid Arrow pages, E native graph/BM25/vector/TurboQuant access, F streamed DataFusion execution, G/H persisted reasoning/context delivery, fixed-hardware performance/recall comparison, signed release assembly, clean-machine installation, and promotion were not run. C-05a changes the low-level reader/fallback boundary and its owning/default compilation surfaces; it does not qualify those later gates, and no C-05 status change is made
remaining known errors: C-05 remains open. The next bounded package is C-05b: fully trace POAM-018's defaulted EstateDocument and EstateBackupJob/recovery-policy shapes, preserve required current semantics in one required schema, replace successful missing-field fixtures with explicit rejection, and prove fresh estate/backup/recovery behavior before deletion. The complete opener audit also still identifies rrd-cluster direct rrd_lsm access under POAM-023 and requires its owning convergence rather than concealment. Kernel old-shape residues remain assigned by the mandatory convergence matrix; the vector catalogue v1/suppression path remains explicitly owned by E-04/E-05/H-01/J-01. C-06 still replaces row segment v3 with the hybrid ordered-spine/Arrow-page format, and E through J remain open
roadmap checkbox changed: no; C-05 remains unchecked, Gate C remains 4/7, and POAM-003 moves only to Verifying because this package removes its named low-level readers but does not prove whole-executable closure
```
