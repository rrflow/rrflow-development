# C-05e vector artifact catalogue v1 removal evidence journal

**Status:** active immutable change-journal evidence
**Coordinate:** `rrflow://rrflow-instance/data/evidence/change-journal/gate-c/c-05e-vector-artifact-catalogue-v1-removal-evidence-journal`
**Owner:** the unchanged historical package receipt identified by its legacy heading and payload digest
**Legacy source:** `docs/roadmap/rrflow-1.0-execution-map.md@b7b061900b67d535ffec2710e6e4c30815d0f216#L2618`
**Legacy payload SHA-256:** `291255e88cbcaa7f1514bc9c2b6d17c94ba4161d38ffd71746ec798c8b32b255`

This record refracts one completed package receipt out of the former
monolithic execution map. Its fenced payload is preserved byte for byte.
It reports historical evidence and cannot change roadmap completion or
POA&M lifecycle status. Follow the [canonical roadmap](../../../roadmap/rrflow-1.0.md)
for acceptance and the [change-journal index](../) for discovery.

## Journal payload

```text
gate/package: C-05e / vector artifact catalogue v1 removal; implemented and verified without closing C-05
starting revision: a905c04156689cebb28f8f3f47dad01ae7759431; tree 0b2b361159cba8620003e2bf50f7d8289a6f57e1
files read in full before editing: repository instructions; README.md; docs/objectives/rrflow-1.0-alpha.md; docs/roadmap/rrflow-1.0.md; docs/poam/rrflow-1.0-alpha.md; the C-05 execution-map chain; crates/compute/rrd-vector/src/catalog.rs; crates/authority/rrd-engine/tests/runtime_vector_artifact_catalog.rs; docs/reference/vector/search.md; the current rrflowKV physical-format reference. Every VectorArtifactCatalogEntry constructor, serializer, validator, digest, public-record encoder, engine replay caller, and source hit was inspected; relevant sections of rrd-vector runtime.rs and rrd-engine runtime/vector_catalog.rs, engine/vector/index.rs, engine/vector/search.rs, and engine/diagnostic.rs were read to classify but not alter the remaining catalogue paths
changed paths and behavior: crates/compute/rrd-vector/src/catalog.rs removes LEGACY_VECTOR_ARTIFACT_CATALOG_VERSION, accepts only VECTOR_ARTIFACT_CATALOG_VERSION 2, and hashes one identity tuple including build_evidence; its negative regression forges a digest-consistent v1 entry and requires rejection. crates/authority/rrd-engine/tests/workspace_architecture.rs pins vector artifact catalogue v2 and rejects revival of the v1 constant. README.md, docs/roadmap/rrflow-1.0.md, docs/poam/rrflow-1.0-alpha.md, and docs/reference/vector/search.md now distinguish this removed reader from the three remaining successful pre-release shapes and describe the direct stamped vector-read path rather than the retired cursor-zero reconstruction. docs/roadmap/rrflow-1.0-file-plan.jsonl was deterministically regenerated. No file was created, deleted, moved, aliased, or given a fallback
test-first surfaced failures: the first negative fixture failed before reaching the intended assertion because its object receipt key was not the required content-addressed canonical key; the fixture was corrected through ObjectReference::canonical_key. Against the unmodified reader, the corrected negative test then failed because validate returned Ok for an internally digest-consistent v1 entry. After direct removal, the same test passed
smallest and owning evidence: `cargo test -p rrd-vector pre_1_0_vector_artifact_catalogue_entries_are_rejected --locked -- --exact` passed 1/1; `cargo test -p rrd-vector catalog_enforces_cas_generation_retirement_and_quarantine --locked -- --exact` passed 1/1; `cargo test -p rrd-engine --test runtime_vector_artifact_catalog rrflow_kv_reopen_reconstructs_catalog_and_missing_bytes_fail_closed --locked -- --exact` passed 1/1; `cargo test -p rrd-engine --test runtime_vector_artifact_catalog catalog_publication_is_logically_identical_across_rrflow_mx_and_rrflow_kv --locked -- --exact` passed 1/1; the exact workspace architecture guard passed 1/1; `cargo test -p rrd-vector --all-targets --locked` passed every unit, integration, benchmark, recall, and example target with no failure; `cargo test -p rrd-engine --test runtime_vector_artifact_catalog --locked` passed 6/6
complete workspace evidence: `cargo test --workspace --all-targets --locked --no-fail-fast` exited 0 with no failed target, including the four-case real-process lifecycle kill/restart matrix; `cargo clippy --workspace --all-targets --locked -- -D warnings` exited 0; `cargo check --workspace --all-targets --locked` exited 0
source and policy evidence: `rg -n 'LEGACY_VECTOR_ARTIFACT_CATALOG_VERSION|contract_version == 1|contract_version == LEGACY|LEGACY.*CATALOG' crates/compute/rrd-vector/src` returned no match; deterministic inventory generation wrote 909 current/generated/planned path records. The first policy pass surfaced expected inventory drift because this evidence journal followed generation and one Rustfmt-only assertion collapse; regeneration raced that formatting once and was repeated serially. After those mechanical updates, the inventory check, documentation policy, generated-surface parity, workflow policy, frozen-version check, 11 bootstrap-knowledge tests, Ruff lint, Cargo formatting check, and diff check all pass
checks not run: external SDK conformance, performance comparison, corruption fuzzing, C-06 physical-format vectors, and Gate E/F acceptance were not run because this package changes no public protocol, SDK, physical segment, vector algorithm, or Arrow/DataFusion execution behavior
remaining known errors: RuntimeSchemaRegistry still accepts an omitted/empty canonical table map and RuntimeDataSnapshot can use caller-selected model fallback; RuntimeVector still accepts no collection address; runtime reconstruction still loads a second VectorCatalog and suppresses TurboQuant from that path. Current exact JSON/HNSW artifacts still require decoding and candidate materialization; C-06/E-04/F remain responsible for their physical, native-index, recall, and stamped Arrow/DataFusion outcomes
roadmap checkbox changed: no; C-05 remains unchecked, Gate C remains 4/7, POAM-003 remains Verifying, and C-05f is the next bounded package
```
