# C-04b implementation journal

**Status:** active immutable change-journal evidence
**Coordinate:** `rrflow://rrflow-instance/data/evidence/change-journal/gate-c/c-04b-implementation-journal`
**Owner:** the unchanged historical package receipt identified by its legacy heading and payload digest
**Legacy source:** `docs/roadmap/rrflow-1.0-execution-map.md@b7b061900b67d535ffec2710e6e4c30815d0f216#L2363`
**Legacy payload SHA-256:** `b46b3d45ec36f15d61855cec281a82173aa38b96005ef2ea9761bda7150059f4`

This record refracts one completed package receipt out of the former
monolithic execution map. Its fenced payload is preserved byte for byte.
It reports historical evidence and cannot change roadmap completion or
POA&M lifecycle status. Follow the [canonical roadmap](../../../roadmap/rrflow-1.0.md)
for acceptance and the [change-journal index](../) for discovery.

## Journal payload

```text
gate/package: C-04b / repository adoption and write-path validation; package complete, C-04 gate still open
starting revision: 9ad281fab9e8ebd4130ebcbf1fbb9dde11794df5
baseline files/digests: access/mod.rs=54b70bd872ee826e84d04194c1cd53b7542af4fd45062202d6bf0e95f9c47f35; access/semantic_commit.rs=a5353d878bb183afec7e984e7d21bdcf8681691b5da5bafeedbf45d0a797177a; access/versioned_read.rs=93200de9b1d473f16cf0e4b46ca9a9ccc8b1763fd6901610e9964c2ad418416f; keyspaces.rs=8825070016a408171596e5521d8c1f25b193700b3f286a07189cc8f9387602f3; repository/runtime.rs=603c7f8c82bf5b8e701505c457ff4a20990627e4254b56ba652b43737d46c557; tests/direct_read_paths.rs=e2e948695bafe67b6dad5e30a51e8bdd8284ee15c73211a2b394378fbf5dc2ae; tests/semantic_commit_atomicity.rs=a49d15095d6ea9a293f519778cf1f89bc266ab738219ba0c99198adf326a83df; tests/semantic_repository_conformance.rs=08cdc8b07ea9e66eff35a396c99e64bb46307c9297611949ec390eca3fa37875; tests/snapshot.rs=bc1f86aa81005248facde7d02a9faf1214ae2d37750aec9cfbe814a72ab11ae3; execution-map.md=435a295e915fdc4455ef1f4f821844083c2a7c6e303d96f95275b822dfa6c030
files read in full: repository instructions and C-04 owner chain retained from the immediately preceding package; this package completely read rrd-store/src/repository/runtime.rs, src/access/{semantic_commit,versioned_read}.rs, tests/{direct_read_paths,snapshot,bitemporal,unified_data,unified_catalogue,semantic_commit_atomicity,semantic_repository_conformance,native_index_commit}.rs, and the complete C-04 execution package. The relevant RuntimeDataSnapshot reducer, RuntimeLogicalModel classification, key-prefix encoding, read-stamp validation, and accumulator proof sections were additionally traced before design. keyspaces.rs and access/mod.rs had already been read completely at C-04a and were changed only at the reviewed version-prefix/re-export coordinates
files changed/created/deleted/moved: changed rrd-store/src/keyspaces.rs, src/access/{mod,semantic_commit,versioned_read}.rs, src/repository/runtime.rs, tests/{direct_read_paths,semantic_commit_atomicity,semantic_repository_conformance,snapshot}.rs, and this execution record; no file created, deleted, or moved. The C-04b planned-file row was corrected before implementation because its required retirement behavior necessarily owns keyspaces.rs and access/{versioned_read,semantic_commit}.rs
contract or behavior changed: RuntimeVersionedSource now supports one exact non-claim semantic identity without adding a second reader. Component-aligned identity prefixes are checked for overlap with family/kind ranges before storage access. One canonical VersionAddress derivation validates both family and identity keys across records, relations, events, vectors, series, geo, objects, and typed retirements. data_snapshot, graph preview, and data preview now open one physical transaction and reduce only authenticated stamped version entries under a caller-supplied key-examination budget; their old cursor-zero read_changes path is removed. Historical previews remain pinned to the supplied stamp after later commits. Preview mutation digests now begin at the stamped global head rather than the last scope-filtered mutation. Retirement validation constructs the current authenticated stamp, selects only the stamped schema plus exact target histories, and uses a mathematical RFC 9162/tree-size integrity bound rather than an operator workload constant. Proof-node budget exhaustion is preserved as RuntimeReadBudgetExceeded instead of being hidden in a generic kernel error. Explicit read_changes, changes_since, snapshot_changes, archive/changefeed/recovery, and rollback log operations were not changed
smallest test command and result: cargo test -p rrd-store --lib access::versioned_read::tests --locked passed 2 cases including collection/identity overlap rejection; cargo test -p rrd-store --test direct_read_paths --locked passed its exact-oracle/profile/reopen case; cargo test -p rrd-store --test snapshot --locked passed 8 cases including current low-budget rejection and retained-stamp preview isolation
owning package command and result: cargo test -p rrd-store --locked --quiet passed all 171 unit/integration cases. Focused unified_catalogue (5), unified_data (4), semantic_commit_atomicity (2), native_index_commit (4), bitemporal (3), and semantic_repository_conformance (2) suites passed. cargo clippy -p rrd-store --all-targets --locked -- -D warnings passed
cross-boundary command and result: cargo check --workspace --all-targets --locked passed every workspace target. cargo test -p rrd-engine --lib --locked passed all 63 engine cases, including context, CRUD, transaction, native-index, vector/TurboQuant, DataFusion composition, restart, and authorization coverage. cargo test -p rrd-query --test query --locked again passed 14/15 and failed only query.rs:398, whose scheduled C-04c assertion still names full_hash_chain_replay while the validated retained stamp truthfully reports rfc9162_direct_versions
failure/crash/differential evidence: the repository snapshot equals the C-04a authenticated-log oracle over two schema revisions, every data family, typed retirements, and an interleaved foreign scope on rrflowMX and reopened rrflowKV. Retained direct evidence remains 49 point reads, 10 ranges, 68 keys/values, and 15,371 decoded bytes; current evidence remains 89 point reads, 9 ranges, 107 keys/values, and 17,109 decoded bytes. The expanded reopened-rrflowKV exercise increased physical counters by 9 block loads, 16,128 loaded bytes, and 281 filter checks. A budget of 1 fails during stamp validation at observed key 2; a budget of 23 reaches proof loading and still returns the typed failure at observed key 24. The first compile exposed unsupported RuntimeLogicalModel ordering, stale StorageTransaction calls, and incomplete Identity matching; all were corrected at the canonical access boundary. The first semantic-commit atomicity run exposed an old 16-change allowance as an insufficient physical-key budget; tests now name adequate key budgets and low-budget failures explicitly
source closure: repository/runtime.rs and access/semantic_commit.rs contain no read_changes(..., 0, ...), change_page(..., 0, ...), or RuntimeChanges range fallback for snapshot, preview, schema, or retirement validation. Their remaining RuntimeDataSnapshot/RuntimeGraphSnapshot reducers consume direct version selections as the allowed kernel reference reducer. Search still finds cursor-zero normal reads only in rrd-query catalogue and rrd-engine retrieval/vector consumers scheduled for C-04c; rrd-engine rollback remains an explicit allowlisted log operation. accumulator_with missing-state bootstrap remains C-05
not run and reason: full workspace tests, SDK/HTTP/WebSocket/GraphQL/Connectome conformance, C-06 Arrow-page work, F DataFusion streaming, E index-quality/recall comparison, installation/attunement, deployment, benchmark threshold, and release suites were not run because this package changes the rrd-store repository/write-validation adoption boundary. The complete workspace compiled and the focused downstream engine/query suites were run
remaining known errors: C-04c must replace rrflowQL catalogue/execute plans and normal retrieval/vector/context consumers with stamped direct sources, then update the one surfaced query assertion with direct physical-plan evidence. Accumulator bootstrap replay remains C-05 inventory. C-05 through J and every alpha outcome remain open exactly as their owners state
roadmap checkbox changed: no; C-04b is complete evidence only, C-04 remains unchecked, Gate C remains 3/7, and C-04c is the next executable package
```
