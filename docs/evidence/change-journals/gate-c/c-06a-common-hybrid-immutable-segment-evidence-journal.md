# C-06a common hybrid immutable segment evidence journal

**Status:** active immutable change-journal evidence
**Coordinate:** `rrflow://rrflow-instance/data/evidence/change-journal/gate-c/c-06a-common-hybrid-immutable-segment-evidence-journal`
**Owner:** the unchanged historical package receipt identified by its legacy heading and payload digest
**Legacy source:** `docs/roadmap/rrflow-1.0-execution-map.md@b7b061900b67d535ffec2710e6e4c30815d0f216#L2769`
**Legacy payload SHA-256:** `f3620f2bf0161b5c47f61b3da6c3d87498d9c6b131868bf1d90950d7582ed5e1`

This record refracts one completed package receipt out of the former
monolithic execution map. Its fenced payload is preserved byte for byte.
It reports historical evidence and cannot change roadmap completion or
POA&M lifecycle status. Follow the [canonical roadmap](../../../roadmap/rrflow-1.0.md)
for acceptance and the [change-journal index](../) for discovery.

## Journal payload

```text
gate/package: C-06a / common ordered key-version spine plus Arrow-layout immutable page foundation; implemented and verified as partial C-06 evidence; C-06 remains open
starting revision: db0b086750eea2e2b2a765dc71db0c032e3d4454; tree 935a7404475f2cec5b517e87d2230120d36e0fa3; the starting worktree exactly matched development/main and was clean
baseline files/digests: rrd-lsm segment.rs=6061082d496058fb62724fbbdc56ee48ef74096e55778df7bc0d65450d4ffafb, manifest.rs=f6456c11f560d3306d8f3ab277f2eb4e76631d2db8bbf9253c36c90070fd2a0, and database.rs=166763dce219a93e666d51a9e4a86da289a05bef4e39078f96d539129462eb98; rrflowKV current-format=781aa0e67bb8ec138325d084a587446f78d0f5d20729b193d19bdc468f8ed942; engine-data-flow=e35bf7aa730463d5036a4f84618787e27709bacf24ed99a41d1fe3c3e198aa0f; canonical roadmap=5928732e8f3cf92f6b6a08cd5210d342d17378928940a3915acdcb2af508be9a; POA&M=11c7c08f93eefde83b9f5a070a60b5bf8b6ee190b6c6c420799e352975d16c90; execution map=b804497019af2c439bfeb4d81da592fe73d5a02d8f5543785f37aeb2ddaf4eee
files read in full before editing: repository AGENTS.md; README; alpha objective; canonical roadmap; complete POA&M and execution map; engine-data-flow, rrflowKV-current-format, tiered-persistence, persistence-scenario, context-maintenance, and SurrealDB capability records; rrd-lsm Cargo manifest, lib.rs, database.rs, io.rs, manifest.rs, memtable.rs, segment.rs, wal.rs, and every rrd-lsm integration test; rrd-store engine.rs, rrflow_kv.rs, and direct_read_paths.rs; rrd-query query.rs integration test; rrd-engine runtime/query.rs, runtime_query_trace.rs, and workspace_architecture.rs; rrd-core trace.rs; and the complete deterministic inventory generator. Every touched hand-authored source and its formatted diff was reread. Upstream code was not copied
source adaptation and research disposition: Apache Arrow defines the physical columnar buffer and validity conventions; DataFusion's TableProvider contract remains a later streaming integration boundary rather than a storage writer; WiscKey-style value separation remains an optional measured policy because its garbage collection and range-scan costs are not free; partitioned Elias-Fano/PFOR remain per-posting candidates for E-03; TurboQuant_prod remains an authenticated approximate representation with exact reranking under E-04; and an LSM-VEC-style disk graph remains a later candidate after the exact/HNSW baseline. Ebbinghaus-like retention may supply evidence to an authorized retirement proposal but never becomes a compaction deletion verdict. Prompt or reasoning boundaries do not force WAL flushes, global reindexing, codebook retraining, or graph rewrites
files changed/created/deleted/moved: replaced rrd-lsm src/segment.rs directly with src/segment/mod.rs and src/segment/format.rs; replaced manifest-v2.json with manifest-v3.json; added segment-v4.hex; regenerated snapshot-bundle-v1.hex; changed rrd-lsm dependency/lock data, database/I/O/lib/manifest code and segment/MVCC/manifest/snapshot/tiered tests; changed rrd-store physical evidence, rrflowKV wiring, and direct-read proof; changed rrd-query and rrd-engine cross-boundary evidence tests and engine trace emission; changed rrd-core's closed direct-convergence attribute inventory; updated README, objective, roadmap, POA&M, architecture, storage/context/research/test-plan records, the roadmap index, this journal, inventory generator, and deterministic file inventory. No public endpoint, SDK contract, adapter, installation behavior, product version, binary, release artifact, or official-repository state changed
physical contract changed: segment v4 is the only accepted segment reader/writer and manifest v3 is the only accepted manifest reader/writer. A row group stores the sorted (key ascending, sequence ascending) MVCC spine in six 64-byte-aligned Arrow-layout pages: little-endian i64 key offsets, key bytes, little-endian u64 sequences, LSB-first value validity, little-endian i64 value offsets, and value bytes. Page descriptors authenticate physical/logical type, encoding, compression, row interval, null count, offset, length, statistics, and SHA-256; manifests authenticate segment-format, schema, key-codec, and page-format identities. One key's complete version chain is never split at a row-group target. Point/range/MVCC/compaction reads use the spine and pages directly and never invoke DataFusion
ownership and evidence changed: current pages use explicit no-compression metadata. mmap page buffers borrow from an Arc-owned mapping; bounded and io_uring page reads use aligned Arrow-owned buffers; snapshot-envelope validation copies. Physical evidence reports page read, decoded, borrowed, allocated, copied, decompressed, cache, filter, and I/O-mode dimensions. The runtime trace retains the six byte dimensions while omitting redundant page-load and aggregate-read attributes so it remains within the frozen 64-attribute bound; per-mode reads and cache counters preserve attribution
replacement and rejection proof: v1/v2/v3 segment bytes and v1/v2 manifest bytes fail closed with no forwarding reader or migration path. The old LZ4 segment dependency, row-record decoder, BlockCache names, and old block counter aliases are absent. The checked-in v4 bytes, v3 manifest, and snapshot bundle pin the current representation. Whole-file and page digests, header/descriptor/range validation, zero alignment-padding validation, and post-open tamper checks reject altered bytes
test-first surfaced failures and corrections: the first v4 suite exposed an incomplete validity bitmap for all-tombstone groups and compaction metadata inheriting a durable watermark instead of deriving the output segment's actual maximum sequence; both were corrected at their physical owner. Cross-boundary direct-read and query tests then showed that reopen checkpoint reconciliation can legitimately warm a one-page fixture: the store-level cold-I/O proof now opens with zero page-cache capacity, while the query-level proof accepts either a page read or bounded-cache hit and still requires the immutable filter path. The engine trace suite exposed more than 64 attributes after adding ownership dimensions; redundant page-load and total-read signals were removed without increasing the bound or dropping the six required byte dimensions. Strict Clippy exposed a manual range predicate, an eight-argument page helper, fixed-width chunk iteration, and a meaningless minimum operation; these were corrected with RangeInclusive::contains, a typed PageStatistics input, fixed array chunks, and direct zero assignment, with no lint suppression or byte change. The deterministic inventory first rejected the unstaged candidate tree, then exposed that F-05 had mistakenly been removed as a duplicate; the sole F-05 cache/invalidation gate was restored before inventory generation. Documentation policy then required the current-format record to state explicitly that v4 is not the accepted RRFlow 1.0 target; that current/target boundary and the exact manifest-reopen validation behavior were corrected before final policy verification
focused and owning proof: cargo test -p rrd-lsm --locked passed 77 unit/integration cases across compaction, failure injection, manifest, MVCC, segment, snapshot, tiered I/O, and WAL suites both before and after the lint corrections; the frozen v4 vector stayed exact. The renamed mmap/bounded ownership test passed separately. cargo test -p rrd-store --locked passed its complete unit/integration/doc suite, including direct version reads, MX/KV equivalence, transaction atomicity, SIGKILL durability, reopen, snapshots, backups, objects, native index deltas, and model soak. cargo test -p rrd-query --locked passed its complete unit/integration/doc suite, including stamped MX/KV reads, DataFusion memory/spill bounds, graph traversal, BM25, GraphQL lowering, temporal reads, and reopen. rrd-engine runtime_query_trace passed 3/3 and workspace_architecture passed 28/28. Strict all-target Clippy passed for rrd-lsm, rrd-store, rrd-query, and rrd-engine with warnings denied; cargo check --workspace --all-targets --locked passed. Deterministic inventory generation and recheck passed with 909 current/generated/planned records; documentation ownership passed with 90 statuses and 88 classified coordinates; generated-surface parity retained 33 HTTP operations at OpenAPI SHA-256 1d18655aa6e670abd7319c3984dfc36b8ed50c280ce8afb62322a5e062b14cc6; workflow policy and frozen 1.0.0 version policy passed. Two knowledge exports were byte-identical at SHA-256 60c5768c83dffe3c0d87fd9935863b979161c985584e1df859ea8b8cd8e5a935 with 89 records and 2 exclusions; all 11 knowledge-export tests and Ruff lint passed
checks not run and reason: full workspace tests, optional distributed/OpenRaft features, six external installed-SDK conformance, Connectome, D installation/attunement, E native graph/BM25/vector acceptance, F streamed-provider and unified-budget acceptance, G/H persisted reasoning/context delivery, I routines/skills, C-07 crash/lifetime completion, fixed-hardware comparison, signed bundle assembly, clean-machine installation, and official promotion were not run or claimed. This is a low-level C-06 slice and cannot qualify those later or optional surfaces
remaining known errors: C-06 still lacks provider-facing query-path selective projection from rrflowQL, configurable byte/row-group budgets, property/fuzz differential coverage, mixed semantic-family interference evidence, mapped-buffer lifetime across generation deletion, and fixed-hardware comparisons for compression, value placement, persisted filters, and cache admission. rrflowQL still materializes semantic QueryRow values before Arrow conversion; F-01 owns the stamped streaming TableProvider. C-07, D, E, F, G, H, I, J, every alpha outcome, turnkey installation, real release binaries, and Connectome integration remain open under their owning records
roadmap checkbox changed: no; C-06 remains unchecked and active, Gate C remains 5/7, POAM-002 remains Open, and no alpha outcome or later gate is promoted by this evidence
```
