# Rrflow.Rrd.Client

Alpha .NET 10 client for the RRFlow Durable Runtime v1 HTTP contract.

The client derives its operation catalogue from RRD's authoritative OpenAPI
document and currently supports credential-free loopback HTTP. Remote transport
remains disabled until the TLS/distributed qualification gate is complete.
