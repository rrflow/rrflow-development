[package]
name = "rrd-vector"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Exact vector oracle, immutable projections, HNSW, and search planner for Rrd."

[features]
default = []
accelerator = []

[dependencies]
rrd-core = { path = "../kernel/rrd-core" }
rrd-store = { path = "../rrd-store" }
serde = { workspace = true }
serde_json = { workspace = true }
memmap2 = "0.9"

[dev-dependencies]
tempfile = "3"
