[package]
name = "rrflow-edge"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Thin no-network deployment face for the RRD edge profile."

[dependencies]
rrd-engine = { path = "../authority/rrd-engine", default-features = false, features = ["edge"] }
serde_json = { workspace = true }

[dev-dependencies]
tempfile = "3"
