[package]
name = "rrd-maintenance"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Governed RRFlow context-maintenance state machine and active projection contract."

[dependencies]
rrd-contract = { path = "../../transport/rrd-contract" }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = "2"
rrd-core = { path = "../../kernel/rrd-core" }
rrd-store = { path = "../../persistence/rrd-store" }

[dev-dependencies]
tempfile = "3"
