[package]
name = "rrd-contract"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Versioned public wire contracts for the RRFlow durable runtime."

[dependencies]
base64 = "0.22"
serde = { workspace = true }
serde_json = { workspace = true }
sha2 = "0.11"
schemars = "1.2"

[dev-dependencies]
rrd-core = { path = "../rrd-core" }
